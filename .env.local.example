# Telegram bot for contact form delivery
# Create a bot via @BotFather, copy the token here
TELEGRAM_BOT_TOKEN=

# Chat ID of the recipient (your private chat, or a group's negative chat ID)
# Reach it via: https://api.telegram.org/bot<TOKEN>/getUpdates → result[].message.chat.id
TELEGRAM_CHAT_ID=
